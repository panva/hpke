{
  "commit-all": true,
  "scripts": {
    "prerelease": "node --run format && node --run docs && git diff --exit-code && node --run test && node --run build",
    "postbump": "node tools/sync-noble-version.js && git add examples/noble-suite/package.json index.js index.d.ts index.d.ts.map -f",
    "postchangelog": "node ./tools/postchangelog.cjs"
  },
  "types": [
    { "type": "feat", "section": "Features", "hidden": false },
    { "type": "fix", "section": "Fixes", "hidden": false },
    { "type": "chore", "hidden": true },
    { "type": "docs", "section": "Documentation", "hidden": false },
    { "type": "style", "hidden": true },
    { "type": "refactor", "section": "Refactor", "hidden": false },
    { "type": "perf", "section": "Performance", "hidden": false },
    { "type": "test", "hidden": true }
  ]
}
